# خطة الإصلاح والتنفيذ

## مكتمل

- اعتماد Supabase مصدر الحقيقة للبيانات المنظمة، مع IndexedDB كـ cache وoutbox فقط.
- توحيد حالات المزامنة، وإيقاف رسائل النجاح حتى إقرار Supabase.
- إضافة revisions وoperation IDs وidempotency وtombstones وإعادة المحاولة والتعارضات.
- إضافة واجهة حل التعارض باختيار النسخة السحابية أو إعادة إرسال النسخة المحلية.
- إصلاح حفظ جدول التوقيت حسب القسم ودمج استعمال الزمن الأسبوعي معه.
- فصل قوائم التلاميذ، وقصر `/classes` على الأقسام المسندة.
- تطبيق migrations الإنتاج والتحقق من المنح وRLS وRealtime وtriggers.
- تمرير عمليات الأقسام والتلاميذ والجدول والدمج والحذف والاستيراد عبر مسار تأكيد واضح.
- تحديث جرد الحالة والوثائق ومسارات OAuth الإنتاجية.

## تحقق آلي

- `npm run lint`
- `npm run test -- --run` — 4 ملفات و29 اختباراً ناجحاً.
- `npm run build` باستخدام Webpack.
- `git diff --check`

## مؤجل باختبار يدوي

Google OAuth الحقيقي، انقطاع الشبكة، تسجيل الخروج مع outbox معلّق، رفع وحذف
الصورة وملفات PDF، استيراد ملفات Excel الحقيقية، النقرات السريعة، واختبارات
الهاتف وRTL وإمكانية الوصول والطباعة وتصدير Word.

## ملاحظات تشغيلية

- لا يتم حذف الفهارس اعتماداً على التخمين؛ يلزم قياس فعلي من `pg_stat_user_indexes`.
- Leaked Password Protection مستبعد بناءً على طلب المستخدم.
- يجب إعادة تشغيل التحقق بعد أي تعديل، ثم النشر النهائي فقط بعد اكتمال المراجعة.
