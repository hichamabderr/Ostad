

# Supabase public client configuration.
# Use the publishable key only in browser/server application code.
NEXT_PUBLIC_SUPABASE_URL="https://YOUR_PROJECT_REF.supabase.co"
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY="sb_publishable_..."
# Temporary local diagnostic; keep false or unset in production.
NEXT_PUBLIC_SUPABASE_EGRESS_DEBUG="false"
# Optional canonical URL used for OAuth callbacks in deployed environments.
# Example: https://ostady.vercel.app
NEXT_PUBLIC_APP_URL="https://ostady.vercel.app"

# Server-only secret. Never expose this through NEXT_PUBLIC_* or client components.
# SUPABASE_SERVICE_ROLE_KEY="sb_secret_..."

# Set to "true" to show the "Coming soon" maintenance page to all visitors
# except the site admin (who can still sign in via the "دخول" link).
NEXT_PUBLIC_MAINTENANCE_MODE="false"
